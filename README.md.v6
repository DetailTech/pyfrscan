A simple Python RF frequency scanner.
pyfrscan v0.6
DetailTech

Introduction
Scans the GMRS frequency range, and outputs to the terminal the timestamp, frequency and power level of signals within the freq range that exceed the squelch. Uses RTL-SDR.

Usage
Install dependencies (!!not identified!!)

Set freqency range, amp, and squelch in the python script.

Run with Python 3.

Ctrl+c to exit.

Tested on Raspberry Pi 4 running Ubuntu 22 LTS.
